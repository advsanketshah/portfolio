(function(){
  // Desktop: dropdowns are CSS :hover — no JS needed.
  // Mobile: navigation is the bottom tab bar (.btab-bar) embedded in each page HTML.
  // No hamburger, no overlay, no injection needed.
})();
